import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'XDK_ionic_capcitor',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
